# fuel-price-lakehouse

A Bronze/Silver/Gold batch pipeline over Brazil's national fuel price survey,
built with PySpark and Delta Lake, with data quality checks that stop the run
instead of logging a warning.

Work in progress.
